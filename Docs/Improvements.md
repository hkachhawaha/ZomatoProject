In location we should show user the dropdown from which we can select the locations.
In budget, the numerical value we can input rather than the low, medium, high (change the logic in the prompt as well)
shortlist size (max rows sent to the model) is not required.
we can fix the amount of retaurants we are sending to the model. 